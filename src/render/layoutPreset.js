/**
 * 页面布局预设。
 *
 * 统一约定：
 * 1. 所有坐标单位都是 px
 * 2. 坐标原点在整张海报左上角
 * 3. x / y 默认表示“左上角位置”
 * 4. width / height 表示区域尺寸
 * 5. size 表示字号或正方形边长
 * 6. 当前画布逻辑尺寸固定为 1080 x 2400
 *
 * 这个文件只负责“摆放位置”和“尺寸参数”：
 * - 元素画不画：由 renderer 决定
 * - 数据内容是什么：由 state 决定
 * - 用户在控制台里改的地图缩放/平移：属于 map transform，不在这里
 */
export const LAYOUT_PRESETS = {
  'keep-walk-v1': {
    /**
     * 整张海报的逻辑画布尺寸。
     * 后面所有坐标都基于这个尺寸来计算。
     */
    canvas: {
      width: 1080,
      height: 2400,
    },

    statusBar: {
      cover: {
        x: 0,
        y: 0,
        width: 1080,
        height: 68,
      },
    },

    /**
     * 顶部系统时间（左上角 22:36 那种）
     * x / y：文字左上角
     * size：字号
     */
    clock: {
      x: 50,
      y: 35,
      size: 20,
    },

    /**
     * “户外行走”这一行。
     *
     * cover：
     * - 用来盖住模板里原本自带的运动图标/标题区域
     * - 因为原模板不是你现在要的版本，所以先盖住，再重画
     *
     * icon：
     * - 行走图标的位置和尺寸
     * - x / y 是图标左上角
     * - width / height 是图标绘制尺寸
     *
     * text：
     * - “户外行走”文字的位置和字号
     * - x / y 是文字左上角
     * - size 是字号
     */
    sportLabel: {
      cover: { x: 28, y: 412, width: 452, height: 108 },
      icon: {
        x: 48,
        y: 410,
        width: 70,
        height: 70,
      },
      text: {
        x: 131,
        y: 423,
        size: 45,
      },
    },

    /**
     * 顶部“隐私”标签。
     * 现在默认可关闭，但布局参数保留。
     *
     * x / y：标签左上角
     * width / height：标签整体尺寸
     * radius：圆角半径
     */
    privacy: {
      x: 633,
      y: 121,
      scale: 0.64,
    },

    aiBadge: {
      x: 633,
      y: 2143,
      scale: 0.99,
    },

    /**
     * 用户资料区（头像 + 昵称 + 日期/地点/天气那一行）
     */
    profile: {
      /**
       * 头像区域。
       * x / y：头像圆形裁切框左上角
       * size：头像显示直径（宽高相同）
       */
      avatar: {
        x: 40,
        y: 245,
        size: 100,
      },

      /**
       * 昵称文字。
       * x / y：昵称文字左上角
       * size：字号
       */
      nickname: {
        x: 160,
        y: 255,
        size: 36,
      },

      /**
       * 日期 / 时间段 / 城市 / 天气 / 温度 那一行。
       * x / y：整行文本起点
       * size：字号
       */
      meta: {
        x: 160,
        y: 305,
        size: 32,
      },
    },

    /**
     * 距离主视觉区（大号公里数 + 后面的“公里”单位）。
     */
    distance: {
      /**
       * 大号数字本身，例如 2.89 / 3.09
       * x / y：大数字左上角
       * size：字号
       */
      value: { x: 50, y: 485, size: 180 },

      /**
       * “公里”单位。
       * y：单位文字左上角纵坐标
       * size：单位字号
       * gap：单位和大数字之间的水平间距
       *
       * 注意：
       * - x 不是在这里写死的
       * - renderer 里会先测量大数字宽度，再根据 gap 计算单位的 x
       * - 所以这里没有 x，只有 y / size / gap
       */
      unit: { y: 590, size: 43, gap: 14 },
    },

    /**
     * 地图显示区域。
     * 这是“地图窗口”的位置和尺寸，不是底图原图的尺寸。
     *
     * x / y：地图卡片左上角
     * width / height：地图卡片显示区域尺寸
     * radius：地图卡片圆角半径
     */
    map: {
      x: 40,
      y: 690,
      width: 1000,
      height: 800,
      radius: 2,
    },

    mapCornerBadge: {
      margin: 24,
      opacity: 0.72,
      scale: 0.65,
    },

    /**
     * 下方“运动数据”区域布局。
     *
     * 这一块控制：
     * - 白色遮罩区域
     * - 3 列排版
     * - 3 行排版
     * - 灰色标题字号
     * - 黑色数值字号
     *
     * 数据顺序目前是：
     * 第一行：训练时长 / 运动消耗 / 步数
     * 第二行：总时长 / 运动负荷 / 平均步频
     * 第三行：平均心率 / 爬升高度 / 平均配速
     */
    lowerData: {
      /**
       * 运动数据区的白色底遮罩。
       *
       * 作用：
       * - 盖住模板原本的数据区内容
       * - 再在上面重绘你现在的数值
       *
       * x / y：遮罩左上角
       * width / height：遮罩尺寸
       */
      overlay: { x: 32, y: 1655, width: 1016, height: 480 },

      /**
       * 三列数据各自的起始 x 坐标。
       * columns[0]：左列
       * columns[1]：中列
       * columns[2]：右列
       */
      columns: [55, 440, 805],

      /**
       * 三行数据的纵坐标。
       *
       * labelY：
       * - 这一行灰色小标题的 y
       *
       * valueY：
       * - 这一行黑色大数值的 y
       */
      rows: [
        { labelY: 1702, valueY: 1752 },
        { labelY: 1872, valueY: 1922 },
        { labelY: 2042, valueY: 2092 },
      ],

      /**
       * 灰色标题字号，例如“训练时长”“步数”
       */
      labelSize: 36,

      /**
       * 黑色数值字号，例如“00:45:04”“4091”
       */
      valueSize: 60,

      /**
       * 运动数据区里“单位”的字号。
       *
       * 当前只单独用于：
       * - 运动消耗的“千卡”
       * - 平均心率的“次/分”
       */
      unitSize: 30,

      /**
       * 单位和前面数值之间的水平间距。
       */
      unitGap: 8,

      /**
       * 单位相对数值顶边的纵向偏移。
       * 用于做视觉对齐。
       */
      unitOffsetY: 20,
    },
  },
};
